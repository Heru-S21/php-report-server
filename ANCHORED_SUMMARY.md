## Progress

### Done
- (previous entries preserved; see git log for full history)
- **Feature 1 — Font Metrics Cache**: `ReportDefinition.fontMetrics` stored in definition JSON; `measureFontMetrics()` returns cached; cleared on element mutations; GET render path passes definition-stored metrics
- **Feature 4 — Visibility Expressions**: `visibleExpression` on BandElement; skip-render check in HtmlRenderer/PdfRenderer (renderSingleElement, renderElementHtml, renderBandElement, calculateEffectiveBandHeight); UI input in ElementEditor Advanced tab
- **Feature 7 — Subtotal / Grand Total Wizards**: context menu on field elements auto-creates footer band + aggregate element (sum/group or sum/report); `findBandByType()` helper; band auto-creation at correct position
- **Feature 3 — Element Alignment & Distribution Tools**: multi-select with Ctrl+Click; `getSelectedElements()`, `alignElements()` (left/right/top/bottom/middle/center), `distributeElements()` (horizontal/vertical); toolbar buttons with disabled state; CSS dashed outline for multi-selected
- **Feature 2 — Undo/Redo History Stack**: single `history` array + index replaces dual-stack; deep-clone snapshots (definition + bands + selection); proper branching; 100-entry limit; all 20+ mutation points instrumented
- **Feature 8 — Barcode / QR Code Element Type**: `picqer/php-barcode-generator` installed; `BandElement` barcode properties; `BarcodeRenderer` (PNG data URI + SVG); `getElementValue` barcode case in both renderers; toolbox button; editor UI (symbology, value expression, show text)
- **Feature 5 — Enhanced Parameter UI**: `dropdown` and `multi-select` parameter types; static options textarea; `dependsOn` cascading; preview renders select/checkboxes; comma-separated multi-select values
- **Font Embedding System**: `src/Api/FontController.php` (index/reload/file); font cache in `data/fonts/cache.json`; routes `GET/POST /api/fonts`, `GET /api/fonts/file/{filename}`; `@font-face` CSS in `HtmlRenderer::getBaseStyles()`; mPDF `fontDir` + `fontdata` in `PdfRenderer`; "Uploaded Fonts" optgroup in `ElementEditor.js` (fetches `/api/fonts`); "Reload Font Cache" button in designer hamburger menu; `updateFontFaceStyles()` for canvas `@font-face`; `/api/fonts/file/` in Auth bypass list; `RenderController::injectFontCache()` passes `_fonts` param to both renderers
- **Settings Expose (3d7e2b9)**: `src/Core/SettingsManager.php` as single source of truth for 18 editable settings with metadata (type, default, group, description); dynamic settings form rendering from API `{definitions, values}` response shape; setting types: bool→checkbox, int→number, select→dropdown, password field, string input; settings grouped into cards (Image Upload, Report Default Margins, Authentication, Date & Number Format, General); database seeds all defaults via `INSERT OR IGNORE` on migrate; margin overrides read from `app_settings` in `PageSettings.php`; fixed `theme.js`, `ImagePicker.js` consumers for new API response shape; fixed settings page theme override on load (removed previewTheme call, syncs from localStorage)
- **Drag-and-Drop GroupField Fix (a301b4f)**: `data-band-group-field` attribute on band DOM elements; `findBandByGroupField(bandEl)` helper for groupField-aware band lookups; `SELECT_BAND_GROUP` dispatch in `app.js` state with `selectedBandGroupField`; all band lookups use type+groupField when groupField is present (drop handler, addElement, element mousedown/resize, band resize, selectBand, pasteElement, object tree, context menu); fixed `addElement()` undefined `bandType` reference (caused aggregateScope to always be 'report'); fixed `BandManager.resizeBand()` to accept optional `groupField` param; fixed `findBandByType()` empty string handling (`!groupField` → `groupField == null`)
- **Category Grouping Feature (7b087bb, d8a66b6)**: `src/Report/CategoryRepository.php` (NEW) — CRUD for `report_categories` table; `src/Api/CategoryController.php` (NEW) — REST endpoints with duplicate/validation checks (GET/POST/PUT/DELETE); routes registered at `src/routes.php`; backend uses scalar subqueries (not LEFT JOINs, avoids duplicate rows); `setCategory()` with transaction safety; `all()` accepts `?category_id` filter; create/update/duplicate/export/import all handle categories; reports list UI with grouped collapsible sections, category tabs bar, inline category assignment per row, add/delete categories with refresh, pencil icon for rename (prompts new name, PUT /api/categories/{id}); designer category dropdown in report properties panel, `reportCategoryId` state, category_id in save payload; CSS variable `--color-bg` added to both themes
- **PDF Backtrack Limit Fix (f129240)**: reverted all experimental PDF changes (chunking at band boundaries, clear:both, position:absolute refactor); kept only `ini_set('pcre.backtrack_limit', '20000000')` before `WriteHTML()` with proper save/restore — fixes 'pcre.backtrack_limit exhausted' error on large reports; mPDF float-based positioning restored to working state
- **Dompdf Engine (5b28f44, 7b7f6ab, 3fd6a89, b2ce531)**: `src/Renderer/DompdfRenderer.php` (NEW, 873 lines) — standalone new file, zero changes to PdfRenderer; `position:fixed` CSS for page headers/footers (no mPDF SetHTMLHeader equivalent); `page-break-before:always` for page breaks; CSS `counter(page)` for page numbers (page count shows "?" — Dompdf doesn't support `counter(total-pages)`); `position:absolute` element layout within `position:relative` row containers; float-based row grouping for element positioning; `@page` CSS with report-defined margins; `@font-face` CSS rules with `file://` paths; `fontFamilyMap` uses original family names (not sanitized); Dompdf chroot includes `data/fonts/` directory; strips nested body from `buildBodies()` to avoid nesting `<html>/<body>`; `'dompdf' => 'Dompdf'` option added to `pdf_engine` setting in SettingsManager; `RenderController::resolvePdfEngine()` reads from app_settings; render/preview select engine accordingly; requires `ext-dom` and `ext-gd` PHP extensions at runtime
- **Dompdf Font Fix (875855b)**: Replace double quotes with single quotes in font-family CSS to prevent HTML `style="..."` attribute breakage when font names contain spaces; adjust usableHeight passed to buildBodies to account for body padding-top/padding-bottom reserved for fixed-position header/footer (prevents content overflowing footer); add 50mm minimum guard clause on bodyHeight to prevent fragmentation on tiny pages
- **Dompdf Parser Fix (1e66cee)**: Disable masterminds/html5 parser (too slow on large documents — DOMDocument handles report HTML correctly and is faster); increase set_time_limit to 120s for large report rendering
- **Dompdf @font-face Style Fix (9fd6aed)**: Map font style from cache.json with match() expression (italic/oblique → italic, else normal) to prevent invalid CSS `font-style:bold` from breaking @font-face rules; remove dead `$fontFamily` variable
- **Dompdf Generic Font Fallbacks (0a406f0)**: Switch all font-family single quotes in fontFamilyCss() to prevent HTML attribute breakage; add serif detection for DejaVu Serif variants; add consolas/monospace detection via str_contains; improved generic fallback chain for system fonts
- **Dompdf System Font & Page Break Fix (0a64063)**: fontFamilyCss now uses only generic families (`monospace, Courier;` / `serif, Times;` / `Helvetica, sans-serif;`) for non-custom fonts to eliminate Dompdf 'Unable to find suitable font replacement' warnings; custom fonts (in fontFamilyMap) retain original name for @font-face matching; add 2mm gutter to bodyHeight for Dompdf rendering tolerance; add `html { margin:0; padding:0; }` CSS reset in getStyles()

### Skipped
- (none intentionally; Feature 6 — Scheduling deferred)

### Blocked
- (none)
